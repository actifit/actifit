<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.actifit.fitnesstracker.actifitfitnesstracker"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Health Connect -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <!--<uses-permission android:name="android.permission.health.READ_HEART_RATE" />-->

    <!--
     <uses-permission android:name="android.permission.health.WRITE_STEPS" />
     <uses-permission android:name="android.permission.health.READ_HEALTH_DATA" />
     <uses-permission android:name="android.permission.health.WRITE_HEALTH_DATA" />

    -->

    <!-- If targeting API 34 and above -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.google.android.apps.healthconnect" />
    </queries>


    <!-- required for QR code scanning -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- required by android 13 / post notifications permission  -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- required by android 13 /  for high sensor usage  -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"/>


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- required as of Android 9 (API level 28) for startforeground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH"/>
    <!--tools:node="remove"/>-->

    <!-- Route map tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
    android:allowBackup="true"
    android:extractNativeLibs="true"
        android:icon="@mipmap/actifit_launcher"
    android:label="@string/app_name"
    android:logo="@mipmap/actifit_launcher"
    android:roundIcon="@mipmap/actifit_launcher_round"
    android:supportsRtl="true"
    android:networkSecurityConfig="@xml/network_security_config"
    android:theme="@style/AppTheme"
    android:name=".ActifitApplication"
    android:requestLegacyExternalStorage="true"
    android:usesCleartextTraffic="true"
    >

        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:label="@string/signup_title" />

        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- Health Connect Provider for Android 13 (API 33) and below -->
        <!-- Required to launch Health Connect app when it's not pre-installed system app -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.health.connect.client.HealthConnectClient"
                android:value="androidx.startup" />
        </provider>

    <!-- request legacy is temporary measure for supporting current image upload/save
        till moving to new android 11 -->

    <!-- [START fcm_default_icon] -->
    <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_icon"
        android:resource="@drawable/actifit_logo" />
    <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_color"
        android:resource="@color/actifitRed" />

    <!-- [END fcm_default_icon] -->
    <!-- [START fcm_default_channel] -->
    <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="@string/actifit_fcm_msg_channel_id" />
    <!-- [END fcm_default_channel] -->

    <activity android:name=".LoginActivity"
        android:theme="@style/Theme.App.Starting"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity android:name=".MainActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />

            <data
                android:host="fitbitcallbackmain"
                android:scheme="actifitcb" /> <!--ftbmaincb-->
        </intent-filter>
    </activity>

    <activity
        android:name=".ShareAchievementActivity"
        android:screenOrientation="portrait"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    <activity
        android:name=".WorkoutWizardActivity"
 android:exported="false"/>

    <activity
        android:name=".EditWorkoutActivity"
        android:exported="false"
        android:label="@string/edit_workout_title"
        android:parentActivityName=".WorkoutWizardActivity">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="io.actifit.fitnesstracker.actifitfitnesstracker.WorkoutWizardActivity" />
    </activity>

    <activity
        android:name=".ExerciseSearchActivity"
        android:exported="false"
        android:label="@string/exercise_search_title"
        android:parentActivityName=".WorkoutWizardActivity"> <!-- Assuming WorkoutWizardActivity is its logical parent -->
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value="io.actifit.fitnesstracker.actifitfitnesstracker.WorkoutWizardActivity" />
    </activity>

    <activity android:name=".StepHistoryActivity" android:exported="false"/>
    <activity
        android:name=".ExerciseDetailActivity"
        android:exported="false"/>
    <activity
        android:name=".PostSteemitActivity"
        android:theme="@style/NoToolbarTheme"
        android:exported="true"
        >
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />

            <data
                android:host="fitbitcallback"
                android:scheme="actifitcb" />
        </intent-filter>
    </activity>
    <activity android:name=".SettingsActivity" />
    <activity android:name=".LeaderboardActivity" />
    <activity android:name=".WalletActivity" />
    <activity android:name=".BodyMetricsActivity" android:exported="false" />
    <activity android:name=".NotificationsActivity" />
    <activity android:name=".DailyDetailedActivity" />
    <activity android:name=".HistoryChartActivity" />
    <activity android:name=".MarketActivity" android:windowSoftInputMode="adjustPan" />
    <activity android:name=".SocialActivity" android:windowSoftInputMode="adjustPan" />
    <activity android:name=".ProfileActivity" android:exported="false" android:label="@string/profile_title" />

    <activity android:name=".RouteMapActivity" android:exported="false" />

    <service
        android:name=".RouteRecordingService"
        android:foregroundServiceType="location"
        android:exported="false" />

    <receiver android:name=".ResetPieChart" />

    <service
        android:name=".ActivityMonitorService"
        android:enabled="true"
        android:exported="false"
        android:foregroundServiceType="health"
        android:description="@string/actifit_service_desc">

    </service>

    <service
        android:name=".ActifitFirebaseMessagingService"
        android:exported="false"

        android:directBootAware="true">
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
    </service>

    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="io.actifit.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths"></meta-data>
    </provider>
    <receiver
            android:name=".MonitorRestartBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="MonitorRestart">
            <intent-filter>
                <action android:name=".MonitorRestart"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!---->
    <receiver
        android:name=".ReminderNotificationService"
        android:enabled="true"
        android:exported="false"
        android:description="@string/actifit_reminder_service_desc">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED" />
        </intent-filter>
    </receiver>

    <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->
    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="@string/admob_app_id"/>

</application>

</manifest>
